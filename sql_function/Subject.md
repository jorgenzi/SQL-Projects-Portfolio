В этом проекте я освоила создание функций и триггеров для реализации бизнес-логики на уровне СУБД, научилась разрабатывать систему аудита изменений данных и оптимизировать запросы.

Задание 00:
Чтобы надежнее работать с данными и не терять события изменений, реализован механизм аудита для операций INSERT.

- Создана таблица person_audit.
- Взята за основу структура таблицы person.

Создан Database Trigger Function с именем fnc_trg_person_insert_audit, который обрабатывает операторы INSERT DML и создаёт копию новой строки в таблице person_audit.

Задание 01:
Определён триггер trg_person_update_audit и соответствующая триггерная функция fnc_trg_person_update_audit для обработки всех операций UPDATE в таблице person.

Задание 02:
Создан триггер trg_person_delete_audit и соответствующая ему триггерная функция fnc_trg_person_delete_audit.

Задание 03: 
Вся логика триггерных функций объединена в один основной триггер с именем trg_person_audit и новую соответствующую триггерную функцию fnc_trg_person_audit.

Другими словами, весь DML-трафик (операции INSERT, UPDATE, DELETE) обрабатывается одним функциональным блоком.

Задание 04:
Определены, 2 SQL-функции с именами:

- fnc_persons_female (должна возвращать женщин),
- fnc_persons_male (должна возвращать мужчин).

Для проверки и вызова функции можно использовать запрос следующего вида:

    SELECT *
    FROM fnc_persons_male();

    SELECT *
    FROM fnc_persons_female();

Задание 05:
Удалены две функции из Задания 4.  
Написана универсальная SQL-функция с именем fnc_persons.  
Эта функция имеет входной параметр pgender со значением по умолчанию 'female'.

Чтобы проверить функцию, вы можете выполнить такой запрос:

    select *
    from fnc_persons(pgender := 'male');

    select *
    from fnc_persons();

Задание 06:
Создана функция fnc_person_visits_and_eats_on_date, основанная на SQL-запросе, которая находит названия пиццерий, которые посетил человек, и где он мог купить пиццу дешевле указанной суммы в рублях (параметр IN pprice со значением по умолчанию 500) на заданную дату (параметр IN pdate со значением по умолчанию 8 января 2022 года).

Чтобы вызвать функцию, выполни запрос, подобный приведенному ниже.

    select *  
    from fnc_person_visits_and_eats_on_date(pprice := 800);

    select *  
    from fnc_person_visits_and_eats_on_date(pperson := 'Anna',pprice := 1300,pdate := '2022-01-01');

Задание 07:
Написана функция на с именем func_minimum, которая принимает входной параметр в виде массива чисел и возвращает минимальное значение.

Для проверки и вызова функции можно использовать запрос, подобный приведенному ниже.

    SELECT func_minimum(VARIADIC arr => ARRAY[10.0, -1.0, 5.0, 4.4]);

Задание 08:
Написана функция с именем fnc_fibonacci, которая принимает входной параметр pstop типа integer (значение по умолчанию - 10) и возвращает таблицу всех чисел Фибоначчи, меньших pstop.

Для проверки работы функции можно выполнить запрос, аналогичный приведенному ниже.

    select * from fnc_fibonacci(100);

    select * from fnc_fibonacci();